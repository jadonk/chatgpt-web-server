{
  "name": "chatgpt-web-server-demo",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  },
  "scripts": {
    "install:browsers": "npx playwright install --with-deps",
    "record": "playwright test --config=playwright.config.ts"
  }
}
